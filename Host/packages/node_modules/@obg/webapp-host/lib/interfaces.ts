import * as express from "express";
import { IServiceProvider } from "@obg/dependency-injection";
import { getCurrentContainer } from "@obg/dependency-injection-express";

export interface IWebApplicationHost {
  initialize(): void;

  extend(parent: express.Application): void;

  start(port: number): void;
}
