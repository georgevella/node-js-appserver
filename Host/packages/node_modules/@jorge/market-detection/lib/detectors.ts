import { IMarketDetector, IMarket } from "./interfaces";
import { Market } from "./Market";
import { Injectable } from "container-ioc";
import * as e from "express";
import { MarketSource } from "./interfaces";

@Injectable()
export class RequestIpMarketDetector implements IMarketDetector {
  readonly index: number;
  detectMarketFromRequest(req: e.Request): IMarket {
    let ip: string;

    return new Market("en", MarketSource.Ip);
  }
}
