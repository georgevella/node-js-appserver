import { IConstructable } from "@jorge/lib-di/lib/interfaces";
import { IContainer } from "container-ioc";
export declare class ContainerConfiguration {
    private items;
    register(token: Symbol, type: IConstructable): void;
    registerSingleton(token: Symbol, type: IConstructable): void;
    registerCollection(token: Symbol, type: IConstructable): void;
    configurationContainer(container: IContainer): void;
}
