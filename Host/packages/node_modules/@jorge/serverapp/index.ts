//import { IContainer } from "container-ioc";
//import { debug } from "util";
import "@jorge/lib-webapphost";
import "@jorge/lib-di";
import "@jorge/app-mobile";
import "@jorge/lib-devicedetection";

import { IServiceProvider, initializeDependencyInjection, getGlobalServiceProvider } from "@jorge/lib-di";
import { IWebApplicationHost, TWebApplicationHost } from "@jorge/lib-webapphost";

initializeDependencyInjection();

var serviceProvider : IServiceProvider = getGlobalServiceProvider();
var host : IWebApplicationHost = serviceProvider.getService(TWebApplicationHost);

host.initialize();
host.start(3000);
